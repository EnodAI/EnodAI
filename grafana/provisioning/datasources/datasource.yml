apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
  
  - name: PostgreSQL
    type: postgres
    url: postgresql:5432
    database: enod_alerts
    user: enod_user
    secureJsonData:
      password: enod_password
    jsonData:
      sslmode: "disable"