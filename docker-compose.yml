version: '3.8'

services:
  postgresql:
    image: postgres:15-alpine
    container_name: enodai-postgresql-1
    environment:
      POSTGRES_USER: enod_user
      POSTGRES_PASSWORD: enod_password
      POSTGRES_DB: enod_alerts
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U enod_user -d enod_alerts"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - enod-net
    restart: always

  redis:
    image: redis:7-alpine
    container_name: enodai-redis-1
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - enod-net
    restart: always

  ollama:
    image: ollama/ollama:latest
    container_name: enodai-ollama-1
    ports:
      - "11434:11434"
    volumes:
      - ollama_data:/root/.ollama
    networks:
      - enod-net
    deploy:
      resources:
        limits:
          memory: 6G
    restart: unless-stopped

  collector:
    build: ./collector
    container_name: enodai-collector-1
    ports:
      - "8080:8080"
    environment:
      - DB_HOST=postgresql
      - DB_USER=enod_user
      - DB_PASSWORD=enod_password
      - DB_NAME=enod_alerts
      - REDIS_ADDR=redis:6379
    depends_on:
      postgresql:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - enod-net
    restart: on-failure

  ai-service:
    build: ./ai-service
    container_name: enodai-ai-service-1
    ports:
      - "8082:8082"
    environment:
      - DB_HOST=postgresql
      - DB_USER=enod_user
      - DB_PASSWORD=enod_password
      - DB_NAME=enod_alerts
      - REDIS_ADDR=redis
      - OLLAMA_URL=http://ollama:11434
    depends_on:
      postgresql:
        condition: service_healthy
      redis:
        condition: service_healthy
      ollama:
        condition: service_started
    networks:
      - enod-net
    restart: on-failure

  prometheus:
    image: prom/prometheus:latest
    container_name: enodai-prometheus-1
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--web.console.libraries=/etc/prometheus/console_libraries'
      - '--web.console.templates=/etc/prometheus/consoles'
      - '--web.enable-lifecycle'
    volumes:
      - ./prometheus/prometheus.yml:/etc/prometheus/prometheus.yml
      - ./prometheus/alert_rules.yml:/etc/prometheus/alert_rules.yml
      - prometheus_data:/prometheus
    ports:
      - "9090:9090"
    networks:
      - enod-net
    restart: always

  grafana:
    image: grafana/grafana:latest
    container_name: enodai-grafana-1
    ports:
      - "3000:3000"
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=enod_password
    volumes:
      - grafana_data:/var/lib/grafana
      - ./grafana/provisioning:/etc/grafana/provisioning
    depends_on:
      - prometheus
    networks:
      - enod-net
    restart: always

networks:
  enod-net:
    driver: bridge

volumes:
  postgres_data:
  redis_data:
  ollama_data:
  prometheus_data:
  grafana_data: