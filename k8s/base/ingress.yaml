apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: enodai-ingress
  namespace: enodai
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
spec:
  tls:
  - hosts:
    - enodai.example.com
    - api.enodai.example.com
    - grafana.enodai.example.com
    secretName: enodai-tls
  rules:
  - host: enodai.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: collector
            port:
              number: 8080
  - host: api.enodai.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: ai-service
            port:
              number: 8082
  - host: grafana.enodai.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: grafana
            port:
              number: 3000
